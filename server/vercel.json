{
    "version": 2,
    "builds": [
        {
            "src": "index.js",
            "use": "@vercel/node"
        },
        {
            "src": "server/package.json",
            "use": "@vercel/node",
            "config": {
                "buildCommand": "npm run vercel-postbuild",
                "outputDirectory": "client/build"
            }
        }

    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "index.js"
        }
    ]
}