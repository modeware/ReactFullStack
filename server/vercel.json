{
    "version": 2,
    "builds": [
        {
            "src": "index.js",
            "use": "@vercel/node"
        },
        {
            "src": "server/client",
            "use": "@vercel/node",
            "config": {
                "buildCommand": "npm install && npm run build",
                "outputDirectory": "client/build"
            }
        }

    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "index.js"
        }
    ]
}