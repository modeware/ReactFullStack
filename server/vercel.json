{
    "version": 2,
    "builds": [
        {
            "src": "index.js",
            "use": "@vercel/node",
            "config": {
                "buildCommand": "npm run vercel-postbuild",
                "outputDirectory": "server/client/build"
            }
        }


    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "index.js"
        }
    ]
}